#include <iostream>
using namespace std;

class Solution {
private:
	struct ListNode {
		int value;
		struct ListNode *next;
		struct ListNode(int x) : value(x), next(NULL) {}
	};

public:
	ListNode* addTwoNumbers(ListNode* l1, ListNode* l2) {
		ListNode *nodePtr;
		nodePtr = head;
		ListNode *sum;
		sum.value = l1.value + l2.vaue;
		while
	}
};

int main() {
	Solution 
}